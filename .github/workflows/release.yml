name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    uses: ./.github/workflows/_build.yml
    with:
      publish: true
      version: ${{ github.ref_name }}
      prerelease: false
    secrets: inherit
